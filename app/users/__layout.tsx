const UsersLayout = ({children}: {children: React.ReactNode}) => {
    return (
        <div>
                <h1 style={{color: "red"}}>Users Layout</h1>
                {children}
        </div>
    )
}

export default UsersLayout
